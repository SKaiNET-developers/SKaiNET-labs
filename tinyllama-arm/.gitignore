.gradle/
.gradle-home/
.kotlin/
build/
*.log
*.hprof
.DS_Store

models/*.gguf
benchmarks/python/models/
benchmarks/python/.venv/

# Compiled IREE artifacts (regenerated by scripts/export-*.sh; the int8 .irpa is 1.1 GB)
*.irpa
*.vmfb

# Python bytecode (scripts/*.py helpers)
__pycache__/
*.pyc

# Agent tool state. settings.local.json records board hostnames and ssh/scp command
# allowlists — it must never be committed. Previously excluded only by the author's
# *global* gitignore, which does not follow the repo to another machine.
.claude/
